# Test workers and fixtures are shipped for reproducibility, not as HTTP endpoints.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
