--- assets/runtime/deferred-runtime-v5.3.210-rc2-hf23.js
+++ assets/runtime/deferred-runtime-v5.3.210-rc2-hf23.js
@@ -1,4 +1,4 @@
-/* Stable Needs checkpoint deferred runtime; generated in manifest order. */
+/* HF23 deferred runtime bundle; generated in manifest order. */
 
 /* BEGIN assets/data/formula-registry.v5.3.210-p1.4.js */
 /* Generated P1.4 formula registry. Do not edit by hand. */
@@ -5431,9 +5431,8 @@
       var expected = String(snap[key]);
       var el = document.getElementById('normInput-' + key);
       if (el && String(el.value) === expected) {
-        // Stable checkpoint: programmatic safety clearing must stay semantically empty.
-        // Synthetic input/change events can coerce '' back to numeric 0 in legacy listeners.
         el.value = '';
+        try { el.dispatchEvent(new Event('input', {bubbles:true})); el.dispatchEvent(new Event('change', {bubbles:true})); } catch(_) {}
         changed = true;
       }
       try {
@@ -7612,344 +7611,6 @@
 ;
 /* END assets/js/04-needs-norms.js */
 
-/* BEGIN assets/js/95-needs-checkpoint-v6-next.js */
-/* Nutrition Calculator V6 — stable Needs checkpoint.
-   One bounded layer: accepted Pass 3A.3/3B.2 calculation contract + consultation UI sync.
-   It intercepts only ordinary adult calculations; protected/clinical/life-stage routes remain owned by legacy safety modules. */
-(function(w,d){'use strict';
-  var VERSION='v6-needs-checkpoint-2026-09-11';
-  var STORAGE_KEY='nutritionCalculator.needsPolicy.v1';
-  var REGIONS=['RU','EU','US','CUSTOM'];
-  var AUTHORITIES=['RU_MR_2021','EFSA','DGE_OEGE','US_DRI','CUSTOM'];
-  var KFA=['unknown','kfa_1_4','kfa_1_6','kfa_1_9','kfa_2_2','desired_1_7'];
-  var ENERGY_CONTEXTS=['general_stable','chronic_stable_estimate_only','extreme_training_estimate_only','protected_clinical'];
-  var PROTEIN_PROFILES=['none','dga_2025_2030','sport_aerobic_2025','sport_strength_2025','older_function_preserving','older_illness_nutrition_risk','weight_loss_obesity_2025','older_obesity_easo_2026','manual'];
-  var REGION_RULES={
-    RU:{allowed:['RU_MR_2021','CUSTOM'],def:'RU_MR_2021'},
-    EU:{allowed:['EFSA','DGE_OEGE','CUSTOM'],def:'EFSA'},
-    US:{allowed:['US_DRI','CUSTOM'],def:'US_DRI'},
-    CUSTOM:{allowed:['CUSTOM'],def:'CUSTOM'}
-  };
-  var ACTIVITY={
-    sedentary:{key:'inactive',label:'Минимальная',pal:'1,00–<1,53',help:'Преимущественно сидячий день, без регулярной заметной двигательной нагрузки.'},
-    low:{key:'low_active',label:'Низкая',pal:'1,53–<1,68',help:'Есть повседневная ходьба и движение, но без большой суммарной нагрузки.'},
-    moderate:{key:'active',label:'Активная',pal:'1,68–<1,85',help:'Регулярная двигательная нагрузка или физически активный день.'},
-    veryhigh:{key:'very_active',label:'Очень высокая',pal:'1,85–<2,50',help:'Большой суммарный объём ежедневной физической активности.'},
-    high:{key:'very_active',label:'Очень высокая',pal:'1,85–<2,50',help:'Миграция прежней категории «Высокая» в NASEM very active.'}
-  };
-  var TARGETS={
-    dga_2025_2030:{lo:1.2,hi:1.6,status:'AMBER',source:'protein.dga_2025_2030'},
-    sport_aerobic_2025:{lo:1.2,hi:1.7,status:'PASS',source:'protein.sport_aerobic_2025'},
-    sport_strength_2025:{lo:1.6,hi:2.2,status:'PASS',source:'protein.sport_strength_2025'},
-    older_function_preserving:{lo:1.0,hi:1.2,status:'AMBER',source:'protein.older_function_preserving'},
-    older_illness_nutrition_risk:{lo:1.2,hi:1.5,status:'CONTEXTUAL',source:'protein.older_illness_nutrition_risk'}
-  };
-  var state={
-    normativeRegion:'RU',referenceAuthority:'RU_MR_2021',ruKfaGroup:'unknown',
-    energyContext:'general_stable',proteinTargetProfile:'none',
-    normativeRegionStatus:'resolved',referenceAuthorityStatus:'resolved',ruKfaStatus:'resolved',
-    energyContextStatus:'resolved',proteinTargetProfileStatus:'resolved'
-  };
-
-  function own(o,k){return Object.prototype.hasOwnProperty.call(o,k);}
-  function clone(o){var r={},k;for(k in o)if(own(o,k))r[k]=o[k];return r;}
-  function has(arr,v){return arr.indexOf(v)>=0;}
-  function numberOrNull(v){
-    if(v===null||v===undefined)return null;
-    if(typeof v==='string'&&v.replace(/\s/g,'')==='')return null;
-    if(typeof v==='string')v=v.replace(',','.');
-    var n=Number(v);return isFinite(n)?n:null;
-  }
-  function round1(n){return Math.round(n*10)/10;}
-  function bmiOf(h,wgt){h=numberOrNull(h);wgt=numberOrNull(wgt);if(!(h>0&&wgt>0))return null;var m=h/100;return round1(wgt/(m*m));}
-  function regionDefault(r){return REGION_RULES[r]?REGION_RULES[r].def:'RU_MR_2021';}
-  function authorityStatus(r,a){var x=REGION_RULES[r];return x&&has(x.allowed,a)?'resolved':'review_required';}
-  function normalizeConfig(patch){
-    patch=patch||{};
-    var next=clone(state),regionChanged=false;
-    if(own(patch,'normativeRegion')){
-      if(has(REGIONS,patch.normativeRegion)){next.normativeRegion=patch.normativeRegion;next.normativeRegionStatus='resolved';}
-      else{next.normativeRegion='RU';next.normativeRegionStatus='review_required';}
-      regionChanged=true;
-    }
-    if(own(patch,'referenceAuthority')){
-      if(has(AUTHORITIES,patch.referenceAuthority))next.referenceAuthority=patch.referenceAuthority;
-      else{next.referenceAuthority=regionDefault(next.normativeRegion);next.referenceAuthorityStatus='review_required';}
-    }else if(regionChanged){next.referenceAuthority=regionDefault(next.normativeRegion);}
-    next.referenceAuthorityStatus=authorityStatus(next.normativeRegion,next.referenceAuthority);
-    if(own(patch,'referenceAuthority')&&!has(AUTHORITIES,patch.referenceAuthority))next.referenceAuthorityStatus='review_required';
-    if(own(patch,'ruKfaGroup')){
-      if(has(KFA,patch.ruKfaGroup)){next.ruKfaGroup=patch.ruKfaGroup;next.ruKfaStatus='resolved';}
-      else{next.ruKfaGroup='unknown';next.ruKfaStatus='review_required';}
-    }
-    if(own(patch,'energyContext')){
-      if(has(ENERGY_CONTEXTS,patch.energyContext)){next.energyContext=patch.energyContext;next.energyContextStatus='resolved';}
-      else{next.energyContext='general_stable';next.energyContextStatus='review_required';}
-    }
-    if(own(patch,'proteinTargetProfile')){
-      if(has(PROTEIN_PROFILES,patch.proteinTargetProfile)){next.proteinTargetProfile=patch.proteinTargetProfile;next.proteinTargetProfileStatus='resolved';}
-      else{next.proteinTargetProfile='none';next.proteinTargetProfileStatus='review_required';}
-    }
-    state=next;return clone(state);
-  }
-  function configure(patch){return normalizeConfig(patch);}
-  function getConfig(){return clone(state);}
-  function resetPatient(){return configure({ruKfaGroup:'unknown',energyContext:'general_stable',proteinTargetProfile:'none'});}
-
-  function nasemEnergy(sex,age,h,wgt,activity){
-    age=numberOrNull(age);h=numberOrNull(h);wgt=numberOrNull(wgt);
-    var a=ACTIVITY[activity]||null;if(!a||!(age>=19&&h>0&&wgt>0))return null;
-    var x;
-    if(sex==='female'){
-      if(a.key==='inactive')x=584.90-7.01*age+5.72*h+11.71*wgt;
-      else if(a.key==='low_active')x=575.77-7.01*age+6.60*h+12.14*wgt;
-      else if(a.key==='active')x=710.25-7.01*age+6.54*h+12.34*wgt;
-      else x=511.83-7.01*age+9.07*h+12.56*wgt;
-    }else{
-      if(a.key==='inactive')x=753.07-10.83*age+6.50*h+14.10*wgt;
-      else if(a.key==='low_active')x=581.47-10.83*age+8.30*h+14.94*wgt;
-      else if(a.key==='active')x=1004.82-10.83*age+6.52*h+15.91*wgt;
-      else x=-517.88-10.83*age+15.61*h+19.11*wgt;
-    }
-    return {kcal:Math.round(x),activityKey:a.key,sourceId:'energy.nasem_2023_adult_'+(sex==='female'?'female_':'male_')+a.key};
-  }
-  function dgeWeight(h,wgt,bmi){
-    if(bmi===null)return {basis:'review_required',weightKg:null};
-    if(bmi<18.5||bmi>=30)return {basis:'review_required',weightKg:null};
-    if(bmi<25)return {basis:'actual',weightKg:wgt};
-    return {basis:'dge_bmi22',weightKg:22*Math.pow(h/100,2)};
-  }
-  function devine(sex,h,wgt){
-    if(!(h>=152.4)||!(sex==='male'||sex==='female'))return {basis:'review_required',weightKg:null,ibwKg:null};
-    var extra=h/2.54-60,ibw=(sex==='male'?50:45.5)+2.3*extra,adj=ibw+0.25*(wgt-ibw);
-    return {basis:'adjusted_25pct_devine',weightKg:adj,ibwKg:ibw};
-  }
-  function targetWeight(profile,sex,h,wgt,bmi){
-    if(profile==='weight_loss_obesity_2025')return {basis:'cap_bmi30',weightKg:Math.min(wgt,30*Math.pow(h/100,2))};
-    if(profile==='older_obesity_easo_2026')return devine(sex,h,wgt);
-    if((profile==='dga_2025_2030'||profile==='sport_aerobic_2025'||profile==='sport_strength_2025'||profile==='older_function_preserving'||profile==='older_illness_nutrition_risk')&&(bmi>=30||bmi<18.5))return {basis:'review_required',weightKg:null};
-    return {basis:'actual',weightKg:wgt};
-  }
-  function proteinReference(cfg,sex,age,h,wgt,bmi){
-    if(cfg.referenceAuthorityStatus!=='resolved')return {available:false,reviewRequired:true,valueG:null,perKg:null,weightKg:null,basis:'review_required',reasonCode:'REFERENCE_AUTHORITY_CONFLICT',authority:cfg.referenceAuthority};
-    var basis={basis:'actual',weightKg:wgt},perkg=null,reason='',source='';
-    if(cfg.referenceAuthority==='RU_MR_2021'){
-      perkg=0.83;reason='RU_MINIMUM_0_83_REFERENCE';source='protein.ru_mr_2021_min_0_83';
-      return {available:true,reviewRequired:false,valueG:perkg*wgt,minimumValueG:perkg*wgt,groupValueG:null,groupKfa:cfg.ruKfaGroup,perKg:perkg,weightKg:wgt,basis:'actual',reasonCode:reason,authority:cfg.referenceAuthority,sourceId:source};
-    }
-    if(cfg.referenceAuthority==='DGE_OEGE'){
-      basis=dgeWeight(h,wgt,bmi);if(basis.basis==='review_required')return {available:false,reviewRequired:true,valueG:null,perKg:null,weightKg:null,basis:'review_required',reasonCode:'DGE_WEIGHT_BASIS_REVIEW_REQUIRED',authority:cfg.referenceAuthority};
-      perkg=age>=65?1.0:0.8;source=age>=65?'protein.dge_older_1_0':'protein.dge_adult_0_8';reason='DGE_REFERENCE';
-    }else if(cfg.referenceAuthority==='EFSA'){
-      if(bmi<18.5||bmi>=30)return {available:false,reviewRequired:true,valueG:null,perKg:null,weightKg:null,basis:'review_required',reasonCode:'EFSA_WEIGHT_BASIS_REVIEW_REQUIRED',authority:cfg.referenceAuthority};
-      perkg=0.83;source='protein.efsa_pri_0_83';reason='EFSA_PRI';
-    }else if(cfg.referenceAuthority==='US_DRI'){
-      if(bmi<18.5||bmi>=30)return {available:false,reviewRequired:true,valueG:null,perKg:null,weightKg:null,basis:'review_required',reasonCode:'US_DRI_WEIGHT_BASIS_REVIEW_REQUIRED',authority:cfg.referenceAuthority};
-      perkg=0.8;source='protein.us_dri_0_8';reason='US_DRI_RDA';
-    }else return {available:false,reviewRequired:true,valueG:null,perKg:null,weightKg:null,basis:'review_required',reasonCode:'CUSTOM_REFERENCE_REQUIRED',authority:cfg.referenceAuthority};
-    return {available:true,reviewRequired:false,valueG:perkg*basis.weightKg,perKg:perkg,weightKg:basis.weightKg,basis:basis.basis,reasonCode:reason,authority:cfg.referenceAuthority,sourceId:source};
-  }
-  function proteinTarget(cfg,sex,age,h,wgt,bmi){
-    var p=cfg.proteinTargetProfile;if(p==='none')return null;
-    if(cfg.proteinTargetProfileStatus!=='resolved')return {reviewRequired:true,status:'review_required',sourceId:null,weightBasis:'review_required'};
-    if(p==='manual')return {manual:true,status:'resolved',sourceId:'protein.manual',weightBasis:'manual'};
-    var wt=targetWeight(p,sex,h,wgt,bmi),def=TARGETS[p];
-    if(wt.basis==='review_required')return {reviewRequired:true,status:'review_required',sourceId:def?def.source:null,weightBasis:'review_required'};
-    if(!def)return {reviewRequired:true,status:'review_required',sourceId:p==='weight_loss_obesity_2025'?'protein.weight_loss_obesity_2025':'protein.older_obesity_easo_2026',weightBasis:wt.basis,weightKg:wt.weightKg,reasonCode:'TARGET_RANGE_REQUIRES_CONTEXT'};
-    return {perKgLow:def.lo,perKgHigh:def.hi,totalLowG:def.lo*wt.weightKg,totalHighG:def.hi*wt.weightKg,status:def.status,sourceId:def.source,weightKg:wt.weightKg,weightBasis:wt.basis,reviewRequired:false};
-  }
-  function waterReference(cfg,sex,maintenance){
-    if(cfg.normativeRegion==='RU')return {kind:'range',lowL:maintenance/1000,highL:maintenance*1.5/1000,scope:'total_water',sourceId:'water.ru_mr_2021_total_per_kcal'};
-    if(cfg.normativeRegion==='EU')return {kind:'ai',valueL:sex==='female'?2.0:2.5,scope:'total_water',sourceId:'water.efsa_total_ai'};
-    if(cfg.normativeRegion==='US')return {kind:'ai',valueL:sex==='female'?2.7:3.7,scope:'total_water',sourceId:'water.us_dri_total_ai'};
-    return {kind:'review',valueL:null,scope:'total_water',sourceId:null};
-  }
-  function calculate(input,cfgOverride){
-    input=input||{};var cfg=cfgOverride?normalizeStandaloneConfig(cfgOverride):getConfig();
-    var age=numberOrNull(input.age),h=numberOrNull(input.heightCm),wgt=numberOrNull(input.weightKg),manual=numberOrNull(input.manualProteinG);
-    var bmi=bmiOf(h,wgt),e=nasemEnergy(input.sex,age,h,wgt,input.activity);
-    if(!e)return {ok:false,reasonCode:'ADULT_NASEM_INPUT_REQUIRED'};
-    var ref=proteinReference(cfg,input.sex,age,h,wgt,bmi),target=proteinTarget(cfg,input.sex,age,h,wgt,bmi);
-    var working=null,workingRange=null,goal=input.goal||'maintain';
-    if(goal==='maintain')working=e.kcal;
-    else if(goal==='gain'||goal==='rehab_gain')workingRange={lowKcal:e.kcal+300,highKcal:e.kcal+500,status:'AMBER',sourceId:'energy.trial_surplus_300_500'};
-    var pWeightBasis=target&&target.weightBasis?target.weightBasis:(ref.basis||'review_required');
-    var activeProtein=null,proteinPerKg=null;
-    if(manual!==null&&manual>0){activeProtein=manual;proteinPerKg=wgt>0?manual/wgt:null;pWeightBasis='manual';}
-    else if(target&&target.reviewRequired!==true&&target.manual!==true){activeProtein=null;}
-    else if(!target&&ref.available){activeProtein=ref.valueG;proteinPerKg=ref.perKg;}
-    var needsPoint=!!(target&&target.reviewRequired!==true&&!target.manual&&manual===null);
-    if(target&&target.manual&&manual===null)needsPoint=true;
-    var configReview=cfg.normativeRegionStatus!=='resolved'||cfg.referenceAuthorityStatus!=='resolved'||cfg.ruKfaStatus!=='resolved'||cfg.energyContextStatus!=='resolved'||cfg.proteinTargetProfileStatus!=='resolved';
-    var targetReview=!!(target&&target.reviewRequired);
-    var macro=working!==null&&activeProtein!==null&&!needsPoint&&!configReview&&!targetReview&&cfg.energyContext==='general_stable';
-    var fat=null,carb=null;if(macro){fat=round1(working*0.30/9);carb=round1(Math.max(0,working-activeProtein*4-fat*9)/4);}
-    var block='';
-    if(cfg.referenceAuthorityStatus!=='resolved')block='reference_authority_review_required';
-    else if(cfg.proteinTargetProfileStatus!=='resolved')block='protein_target_profile_review_required';
-    else if(cfg.energyContextStatus!=='resolved')block='energy_context_review_required';
-    else if(cfg.ruKfaStatus!=='resolved')block='ru_kfa_review_required';
-    else if(targetReview)block='protein_target_review_required';
-    else if(needsPoint)block='protein_working_point_unresolved';
-    else if(working===null)block='working_target_unresolved';
-    else if(cfg.energyContext!=='general_stable')block='energy_context_estimate_only';
-    var audit=[e.sourceId];if(ref.sourceId)audit.push(ref.sourceId);if(target&&target.sourceId)audit.push(target.sourceId);var wr=waterReference(cfg,input.sex,e.kcal);if(wr.sourceId)audit.push(wr.sourceId);
-    return {
-      ok:true,version:VERSION,method:'nasem_checkpoint',bmi:bmi,
-      maintenanceEnergyKcal:e.kcal,workingEnergyTargetKcal:working,workingEnergyRange:workingRange,
-      energyLow:working,energyHigh:working,energyTarget:working,activityKey:e.activityKey,
-      proteinReference:ref,proteinTargetRange:target,proteinTargetProfile:cfg.proteinTargetProfile,
-      proteinTargetNeedsExplicitWorkingPoint:needsPoint,totalProtein:activeProtein,proteinPerKg:proteinPerKg,proteinWeightBasis:pWeightBasis,
-      fatGrams:fat,carbGrams:carb,waterReference:wr,macroCalculationAvailable:macro,
-      normativeRegion:cfg.normativeRegion,referenceAuthority:cfg.referenceAuthority,referenceAuthorityStatus:cfg.referenceAuthorityStatus,
-      ruKfaGroup:cfg.ruKfaGroup,ruKfaStatus:cfg.ruKfaStatus,energyContext:cfg.energyContext,energyContextStatus:cfg.energyContextStatus,
-      proteinTargetProfileStatus:cfg.proteinTargetProfileStatus,normsSyncAllowed:macro,normsSyncBlockReason:block,
-      formulaAuditPass3B2:audit,energyWeightKg:wgt,proteinWeightKg:ref.weightKg||((target&&target.weightKg)||wgt),energyEstimateOnly:cfg.energyContext!=='general_stable'
-    };
-  }
-  function normalizeStandaloneConfig(src){var old=state,copy=clone(state);state=copy;var out=normalizeConfig(src||{});state=old;return out;}
-
-  function safeGet(id){return d&&d.getElementById(id);}
-  function val(id){var e=safeGet(id);return e?e.value:'';}
-  function checked(id){var e=safeGet(id);return !!(e&&e.checked);}
-  function refeedingContextPresent(){
-    var current=numberOrNull(val('needs_w')),prev=numberOrNull(val('needs_prev_w')),days=numberOrNull(val('needs_low_intake_days'));
-    if(days!==null&&days>0)return true;
-    if(prev!==null&&current!==null&&prev>current)return true;
-    if(val('needs_electrolytes')==='low')return true;
-    if(val('needs_refeeding_factors')==='yes')return true;
-    return false;
-  }
-  function ordinaryAdultEligible(){
-    var age=numberOrNull(val('needs_age')),st=val('needs_state')||'normal',guard=val('needs_guardrail')||'none';
-    if(!(age>=19))return false;
-    if(!(st==='normal'||st==='rehab'||st==='elderly'))return false;
-    if(guard!=='none'||val('needs_edema')==='yes')return false;
-    if(checked('needs_additional_ed_risk')||checked('needs_additional_medical_restriction')||checked('needs_additional_clinical_conditions'))return false;
-    if(refeedingContextPresent())return false;
-    return true;
-  }
-  function inputFromDom(){return {sex:val('needs_sex')||'male',heightCm:val('needs_h'),weightKg:val('needs_w'),age:val('needs_age'),activity:val('needs_activity'),goal:val('needs_goal')||'maintain',manualProteinG:val('needs_protein_manual')};}
-  function fmt(n,dig){if(n===null||n===undefined||!isFinite(Number(n)))return '—';return Number(n).toFixed(dig||0).replace(/\.0$/,'');}
-  function proteinHeading(meta){if(meta.totalProtein!==null&&meta.proteinWeightBasis==='manual')return 'Белок — рабочая цель';if(meta.proteinTargetRange&&!meta.proteinTargetRange.reviewRequired)return 'Белок — целевой диапазон';return 'Белок — референс';}
-  function proteinDisplay(meta){var t=meta.proteinTargetRange;if(meta.totalProtein!==null&&meta.proteinWeightBasis==='manual')return fmt(meta.totalProtein,0)+' г';if(t&&!t.reviewRequired&&t.totalLowG!=null)return fmt(t.totalLowG,0)+'–'+fmt(t.totalHighG,0)+' г';if(meta.proteinReference&&meta.proteinReference.available)return fmt(meta.proteinReference.valueG,0)+' г';return 'нужно уточнить';}
-  function waterDisplay(wr){if(!wr)return '—';if(wr.kind==='range')return fmt(wr.lowL,1)+'–'+fmt(wr.highL,1)+' л';if(wr.valueL!=null)return fmt(wr.valueL,1)+' л';return 'нужно уточнить';}
-  function renderReport(meta){
-    var out=safeGet('needs_out');if(!out)return;
-    var energyMain=meta.workingEnergyTargetKcal!==null?fmt(meta.workingEnergyTargetKcal,0)+' ккал':'поддержание '+fmt(meta.maintenanceEnergyKcal,0)+' ккал';
-    var energyNote=meta.workingEnergyTargetKcal!==null?'рабочая энергетическая цель':'рабочая цель пока не выбрана';
-    var protTitle=proteinHeading(meta),prot=proteinDisplay(meta),target=meta.proteinTargetRange;
-    var protNote='';
-    if(meta.proteinWeightBasis==='manual')protNote='задано специалистом вручную';
-    else if(target&&!target.reviewRequired)protNote=fmt(target.perKgLow,1)+'–'+fmt(target.perKgHigh,1)+' г/кг; диапазон сам не превращается в рабочую точку';
-    else if(meta.proteinReference&&meta.proteinReference.available)protNote=fmt(meta.proteinReference.perKg,2)+' г/кг; справочный ориентир, не автоматически оптимальная цель';
-    else protNote='нужно разрешить нормативный или расчётный контекст';
-    var warn=meta.normsSyncAllowed?'':'<div class="alert" style="margin-top:10px"><b>КБЖУ не перенесены автоматически.</b> '+(meta.normsSyncBlockReason==='working_target_unresolved'?'Сначала задайте рабочую энергетическую цель.':meta.normsSyncBlockReason==='protein_working_point_unresolved'?'Для выбранного белкового диапазона нужна конкретная рабочая точка.':'Текущая конфигурация требует проверки специалистом.')+'</div>';
-    out.innerHTML='<div class="needs-checkpoint-results">'+
-      '<div class="needs-checkpoint-result"><span>Энергия</span><strong>'+energyMain+'</strong><small>'+energyNote+' · NASEM 2023</small></div>'+
-      '<div class="needs-checkpoint-result"><span>'+protTitle+'</span><strong>'+prot+'</strong><small>'+protNote+'</small></div>'+
-      '<div class="needs-checkpoint-result"><span>Вода — референс</span><strong>'+waterDisplay(meta.waterReference)+'</strong><small>общая вода из пищи и напитков</small></div>'+
-      '<div class="needs-checkpoint-result"><span>Метод</span><strong>NASEM 2023</strong><small>'+meta.normativeRegion+' · '+meta.referenceAuthority+'</small></div>'+
-      '</div>'+warn+
-      '<div class="small" style="margin-top:10px">Поддерживающая энергия: <b>'+fmt(meta.maintenanceEnergyKcal,0)+' ккал</b>. Расчёт является оценкой и требует калибровки по фактической динамике массы и контексту пациента.</div>'+
-      '<span id="needs_kcal_low" hidden>'+(meta.energyLow==null?'':fmt(meta.energyLow,0))+'</span><span id="needs_kcal_high" hidden>'+(meta.energyHigh==null?'':fmt(meta.energyHigh,0))+'</span><span id="needs_protein_total" hidden>'+(meta.totalProtein==null?'':fmt(meta.totalProtein,1))+'</span><span id="needs_fat_g" hidden>'+(meta.fatGrams==null?'':fmt(meta.fatGrams,1))+'</span><span id="needs_carb_g" hidden>'+(meta.carbGrams==null?'':fmt(meta.carbGrams,1))+'</span>';
-  }
-  function renderSummary(meta){
-    var box=safeGet('consultationNeedsSummary');if(!box)return;
-    if(!meta){box.hidden=true;box.innerHTML='';return;}
-    box.hidden=false;
-    box.innerHTML='<div class="consultation-summary-head"><div><span class="step-badge">Потребности</span><strong>Рабочая сводка</strong></div><small>обновляется после расчёта</small></div><div class="consultation-summary-grid">'+
-      '<div><span>Энергия</span><strong>'+fmt(meta.maintenanceEnergyKcal,0)+' ккал</strong><small>'+(meta.workingEnergyTargetKcal==null?'рабочая цель не выбрана':'поддержание / рабочая цель')+'</small></div>'+
-      '<div><span>'+proteinHeading(meta)+'</span><strong>'+proteinDisplay(meta)+'</strong><small>'+meta.proteinTargetProfile.replace(/_/g,' ')+'</small></div>'+
-      '<div><span>Вода</span><strong>'+waterDisplay(meta.waterReference)+'</strong><small>общая вода</small></div>'+
-      '<div><span>Контекст</span><strong>'+((ACTIVITY[val('needs_activity')]||{}).label||'—')+'</strong><small>'+meta.referenceAuthority+'</small></div></div>';
-  }
-  function safeClearAutoNorms(){
-    var snap=w.__needsAutoNormSnapshotV53210||null;if(!snap)return;
-    if(!w.__needsSafetyUnsetNormKeysV05)w.__needsSafetyUnsetNormKeysV05={};
-    Object.keys(snap).forEach(function(key){var el=safeGet('normInput-'+key);if(el&&String(el.value)===String(snap[key]))el.value='';try{if(w.norms&&w.norms.values&&w.norms.values[key])w.norms.values[key].value=0;}catch(_){}w.__needsSafetyUnsetNormKeysV05[key]=true;});
-    w.__needsAutoNormSnapshotV53210=null;try{if(typeof w.renderTotals==='function')w.renderTotals();}catch(_){}
-  }
-  function applyCalculation(){
-    if(!ordinaryAdultEligible())return false;
-    var activity=val('needs_activity');if(!ACTIVITY[activity]){try{w.alert('Выберите уровень активности.');}catch(_){}return true;}
-    var inp=inputFromDom(),meta=calculate(inp);
-    if(!meta.ok){try{w.alert('Проверьте рост, массу, возраст и активность.');}catch(_){}return true;}
-    safeClearAutoNorms();
-    meta.state=val('needs_state')||'normal';meta.activity=activity;meta.goal=inp.goal;meta.sex=inp.sex;meta.h=numberOrNull(inp.heightCm);meta.w=numberOrNull(inp.weightKg);meta.age=numberOrNull(inp.age);
-    meta.personalProfile={version:VERSION,mode:'adult_nasem_checkpoint',energyKcal:meta.workingEnergyTargetKcal,proteinG:meta.totalProtein};
-    try{w.__lastNeedsMeta=meta;w.__lastPersonalNeedsProfile=meta.personalProfile;w.__lastNeedsProfileApplied=true;if(w.NeedsCalculationStateV53211)w.NeedsCalculationStateV53211.setActionsEnabled(true);}catch(_){}
-    var bmi=safeGet('needs_bmi');if(bmi)bmi.value=meta.bmi==null?'':meta.bmi;
-    renderReport(meta);renderSummary(meta);
-    try{d.dispatchEvent(new CustomEvent('needs:computed',{detail:meta}));}catch(_){}
-    return true;
-  }
-
-  function option(value,text,selected){var o=d.createElement('option');o.value=value;o.textContent=text;if(selected)o.selected=true;return o;}
-  function rebuildActivity(){
-    var e=safeGet('needs_activity');if(!e)return;
-    var old=e.value;if(old==='high')old='veryhigh';
-    while(e.firstChild)e.removeChild(e.firstChild);
-    var prompt=option('','Выберите уровень активности',false);prompt.disabled=true;e.appendChild(prompt);
-    ['sedentary','low','moderate','veryhigh'].forEach(function(k){e.appendChild(option(k,ACTIVITY[k].label+' · PAL '+ACTIVITY[k].pal,old===k));});
-    e.value=has(['sedentary','low','moderate','veryhigh'],old)?old:'';
-    var h=safeGet('needsActivityHint');if(!h){h=d.createElement('div');h.id='needsActivityHint';h.className='small';e.parentNode.appendChild(h);}renderActivityHint();
-  }
-  function renderActivityHint(){var e=safeGet('needs_activity'),h=safeGet('needsActivityHint');if(!e||!h)return;var a=ACTIVITY[e.value];h.textContent=a?a.help:'Выберите категорию активности — калькулятор не назначает её автоматически.';}
-  function selectHtml(id,label,items){var wrap=d.createElement('div'),lab=d.createElement('label'),sel=d.createElement('select'),i;lab.htmlFor=id;lab.textContent=label;sel.id=id;for(i=0;i<items.length;i++)sel.appendChild(option(items[i][0],items[i][1],false));wrap.appendChild(lab);wrap.appendChild(sel);return wrap;}
-  function injectControls(){
-    if(safeGet('needs_protein_target_profile'))return;
-    var activity=safeGet('needs_activity'),grid=activity&&activity.closest?activity.closest('.grid'):null;if(!activity||!grid)return;
-    var p=selectHtml('needs_protein_target_profile','Белковая цель / тренировочный контекст',[
-      ['none','Без специальной цели — показать референс'],['dga_2025_2030','DGA 2025–2030 · 1,2–1,6 г/кг'],['sport_aerobic_2025','Аэробная нагрузка · 1,2–1,7 г/кг'],['sport_strength_2025','Силовая / гипертрофия · 1,6–2,2 г/кг'],['older_function_preserving','Старший возраст · практический 1,0–1,2 г/кг'],['older_illness_nutrition_risk','Старший возраст + нутритивный риск · 1,2–1,5 г/кг'],['manual','Только ручная рабочая цель']]);
-    activity.parentNode.insertAdjacentElement('afterend',p);
-    var det=d.createElement('details');det.id='needsNormativePolicy';det.className='fold';det.style.gridColumn='1/-1';det.innerHTML='<summary>Нормативные источники</summary><div class="small" style="margin:8px 0">Регион и reference authority — настройка специалиста. Российский КФА и белковая цель относятся к конкретному пациенту и не переносятся после «Сбросить».</div><div class="grid" id="needsNormativePolicyGrid"></div>';
-    p.insertAdjacentElement('afterend',det);var g=safeGet('needsNormativePolicyGrid');
-    g.appendChild(selectHtml('needs_normative_region','Регион нормативов',[['RU','Россия'],['EU','Европа'],['US','США'],['CUSTOM','Пользовательская система']]));
-    g.appendChild(selectHtml('needs_reference_authority','Reference authority',[['RU_MR_2021','РФ · МР 2.3.1.0253-21'],['EFSA','EFSA'],['DGE_OEGE','DGE / ÖGE'],['US_DRI','US DRI'],['CUSTOM','Вручную / другой источник']]));
-    g.appendChild(selectHtml('needs_ru_kfa_group','Российская группа КФА',[['unknown','Не выбрана'],['kfa_1_4','КФА 1,4'],['kfa_1_6','КФА 1,6'],['kfa_1_9','КФА 1,9'],['kfa_2_2','КФА 2,2'],['desired_1_7','Желаемый КФА 1,7 (только где применимо)']]));
-    g.appendChild(selectHtml('needs_energy_context','Контекст оценки энергии',[['general_stable','Обычный стабильный взрослый'],['chronic_stable_estimate_only','Стабильное хроническое состояние — только оценка'],['extreme_training_estimate_only','Экстремальная тренировка — только оценка']]));
-    var note=d.createElement('div');note.className='small';note.style.gridColumn='1/-1';note.id='needsPolicyNote';note.textContent='NASEM PAL и российская КФА — разные классификации и автоматически друг в друга не переводятся.';g.appendChild(note);
-    var mp=safeGet('needs_protein_manual');if(mp&&mp.previousElementSibling)mp.previousElementSibling.textContent='Рабочая цель белка, вручную (г/сут)';
-  }
-  function renderPolicy(){
-    var ids={needs_normative_region:state.normativeRegion,needs_reference_authority:state.referenceAuthority,needs_ru_kfa_group:state.ruKfaGroup,needs_energy_context:state.energyContext,needs_protein_target_profile:state.proteinTargetProfile},k,e;
-    for(k in ids)if(own(ids,k)){e=safeGet(k);if(e)e.value=ids[k];}
-    var kfa=safeGet('needs_ru_kfa_group');if(kfa&&kfa.parentNode)kfa.parentNode.style.display=state.normativeRegion==='RU'?'':'none';
-    var note=safeGet('needsPolicyNote');if(note){var bits=[];if(state.referenceAuthorityStatus!=='resolved')bits.push('Выбранная authority несовместима с регионом — автоматический белковый референс заблокирован.');if(state.ruKfaGroup!=='unknown')bits.push('Выбранный КФА сохраняется как контекст пациента; групповой белковый норматив не подменяет отдельный минимум 0,83 г/кг.');note.textContent=bits.length?bits.join(' '):'NASEM PAL и российская КФА — разные классификации и автоматически друг в друга не переводятся.';}
-  }
-  function readClinicianPrefs(){try{var x=JSON.parse(w.localStorage.getItem(STORAGE_KEY)||'null');if(x&&typeof x==='object')configure({normativeRegion:x.normativeRegion,referenceAuthority:x.referenceAuthority});}catch(_){} }
-  function saveClinicianPrefs(){try{w.localStorage.setItem(STORAGE_KEY,JSON.stringify({normativeRegion:state.normativeRegion,referenceAuthority:state.referenceAuthority}));}catch(_){} }
-  function bindControls(){
-    var a=safeGet('needs_activity');if(a)a.addEventListener('change',function(){renderActivityHint();invalidate('activity');},false);
-    var r=safeGet('needs_normative_region');if(r)r.addEventListener('change',function(){configure({normativeRegion:r.value});saveClinicianPrefs();renderPolicy();invalidate('normative_region');},false);
-    var au=safeGet('needs_reference_authority');if(au)au.addEventListener('change',function(){configure({referenceAuthority:au.value});saveClinicianPrefs();renderPolicy();invalidate('reference_authority');},false);
-    var k=safeGet('needs_ru_kfa_group');if(k)k.addEventListener('change',function(){configure({ruKfaGroup:k.value});renderPolicy();invalidate('ru_kfa');},false);
-    var ec=safeGet('needs_energy_context');if(ec)ec.addEventListener('change',function(){configure({energyContext:ec.value});renderPolicy();invalidate('energy_context');},false);
-    var pp=safeGet('needs_protein_target_profile');if(pp)pp.addEventListener('change',function(){configure({proteinTargetProfile:pp.value});renderPolicy();invalidate('protein_profile');},false);
-    var btn=safeGet('needs_calc_btn');if(btn)btn.addEventListener('click',function(ev){if(ordinaryAdultEligible()){ev.preventDefault();ev.stopImmediatePropagation();applyCalculation();}},true);
-    var reset=safeGet('needs_reset_btn');if(reset)reset.addEventListener('click',function(){w.setTimeout(function(){resetPatient();renderPolicy();var act=safeGet('needs_activity');if(act)act.value='';renderActivityHint();renderSummary(null);},0);},false);
-  }
-  function invalidate(reason){try{safeClearAutoNorms();if(w.NeedsCalculationStateV53211)w.NeedsCalculationStateV53211.invalidate('checkpoint:'+reason);}catch(_){}renderSummary(null);}
-  function injectSummary(){if(safeGet('consultationNeedsSummary'))return;var section=safeGet('needsCompact');if(!section)return;var box=d.createElement('section');box.id='consultationNeedsSummary';box.className='card consultation-needs-summary';box.hidden=true;section.insertAdjacentElement('afterend',box);}
-  function syncWorkflow(){var nav=d.querySelector('.workflow-steps');if(!nav)return;nav.innerHTML='<a href="#needsCompact"><span>1</span>Потребности</a><a href="#globalSearchSection"><span>2</span>Рацион</a><a href="#heiPanel"><span>3</span>Анализ</a><a href="#geminiAiSection"><span>4</span>Улучшить</a><a href="#globalActions"><span>5</span>Отчёт</a>';}
-  function injectStyles(){if(safeGet('needsCheckpointStyle'))return;var s=d.createElement('style');s.id='needsCheckpointStyle';s.textContent='html[data-navigation-shell="long"] #mainContent{width:min(100%,1200px)!important;max-width:1200px!important}html[data-navigation-shell="long"] #mainContent>#needsCompact,html[data-navigation-shell="long"] #mainContent>#consultationNeedsSummary{width:100%!important;max-width:none!important;box-sizing:border-box}.consultation-needs-summary{margin:-2px 0 16px}.consultation-summary-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:10px}.consultation-summary-grid,.needs-checkpoint-results{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.consultation-summary-grid>div,.needs-checkpoint-result{padding:11px 12px;border:1px solid var(--line,#d9d9d9);border-radius:12px;background:var(--card,#fff)}.consultation-summary-grid span,.needs-checkpoint-result span{display:block;font-size:12px;opacity:.72}.consultation-summary-grid strong,.needs-checkpoint-result strong{display:block;margin-top:3px;font-size:17px}.consultation-summary-grid small,.needs-checkpoint-result small{display:block;margin-top:4px;line-height:1.35;opacity:.72}.workflow-steps{position:sticky;top:0;z-index:15;overflow-x:auto}.workflow-steps a{white-space:nowrap}@media(max-width:760px){.consultation-summary-grid,.needs-checkpoint-results{grid-template-columns:1fr 1fr}html[data-navigation-shell="long"] #mainContent{width:100%!important;max-width:100%!important}}@media(max-width:430px){.consultation-summary-grid,.needs-checkpoint-results{grid-template-columns:1fr}}';(d.head||d.documentElement).appendChild(s);}
-  function setLongMode(){try{if(w.NavigationShellV1&&w.NavigationShellV1.setMode)w.NavigationShellV1.setMode('long');else d.documentElement.setAttribute('data-navigation-shell','long');}catch(_){d.documentElement.setAttribute('data-navigation-shell','long');}}
-  function updateNotice(){var n=safeGet('needs-method-notice');if(n)n.innerHTML='<b>Обычный взрослый:</b> поддерживающая энергия рассчитывается по NASEM 2023. Белковый референс и целевой профиль показаны отдельно; диапазон не превращается в рабочую точку автоматически. Защищённые клинические, детские, беременность и лактация остаются в специальных контурах.';}
-  function init(){
-    if(!d||w.__NEEDS_CHECKPOINT_MOUNTED__)return;w.__NEEDS_CHECKPOINT_MOUNTED__=true;
-    readClinicianPrefs();rebuildActivity();injectControls();injectSummary();injectStyles();syncWorkflow();updateNotice();renderPolicy();bindControls();setLongMode();
-    w.setTimeout(function(){setLongMode();},120);
-  }
-
-  var api={version:VERSION,configure:configure,getConfig:getConfig,resetPatient:resetPatient,calculate:calculate,nasemEnergy:nasemEnergy,bmi:bmiOf,dgeWeight:dgeWeight,devine:devine,proteinReference:proteinReference,proteinTarget:proteinTarget,activity:clone(ACTIVITY),init:init};
-  w.NutritionNeedsCheckpointV1=api;
-  if(d){if(d.readyState==='loading')d.addEventListener('DOMContentLoaded',function(){if(w.__APP_BOOTSTRAP_META__&&w.__APP_BOOTSTRAP_META__.status==='ready')init();},false);if(w.addEventListener)w.addEventListener('app:ready',init,false);if(w.__APP_BOOTSTRAP_META__&&w.__APP_BOOTSTRAP_META__.status==='ready')init();}
-})(window,document);
-
-;
-/* END assets/js/95-needs-checkpoint-v6-next.js */
-
 /* BEGIN assets/js/72-diet-interpretation-engine-v5.3.210-rc2-hf3.js */
 /* Diet interpretation engine — independent from the HEI score. RC2 HOTFIX3. */
 (function(w,d){'use strict';
@@ -22109,8 +21770,7 @@
   var FEATURE_LEVEL = 7;
   var API = "./api/gemini.php?v=5.3.210-p0.5";
   var STORAGE = "nutri_ai_planner_preferences_v5_3_210";
-  var AI_ATTESTATION_KEY = "nutri_ai_nonclinical_transfer_consent_v2";
-  var LEGACY_AI_ATTESTATION_KEY = "nutri_ai_user_18_attestation_v1";
+  var AI_ATTESTATION_KEY = "nutri_ai_user_18_attestation_v1";
   var serviceSessionPromise = null;
   var activeController = null,
     currentResult = null,
@@ -22500,11 +22160,11 @@
   function aiAttested() {
     var box = $("geminiAiEligibility");
     if (box && box.checked) return true;
-    try { return sessionStorage.getItem(AI_ATTESTATION_KEY) === "1" || sessionStorage.getItem(LEGACY_AI_ATTESTATION_KEY) === "1"; } catch (_) { return false; }
+    try { return sessionStorage.getItem(AI_ATTESTATION_KEY) === "1"; } catch (_) { return false; }
   }
   function setAiAttestation(value) {
     value = !!value;
-    try { if (value) sessionStorage.setItem(AI_ATTESTATION_KEY, "1"); else { sessionStorage.removeItem(AI_ATTESTATION_KEY); sessionStorage.removeItem(LEGACY_AI_ATTESTATION_KEY); } } catch (_) {}
+    try { if (value) sessionStorage.setItem(AI_ATTESTATION_KEY, "1"); else sessionStorage.removeItem(AI_ATTESTATION_KEY); } catch (_) {}
     ["geminiAiEligibility", "geminiRationAiEligibility"].forEach(function (id) { var el=$(id); if(el) el.checked=value; });
   }
   function aiUsageContext() {
@@ -22517,9 +22177,7 @@
     var confirmed=aiAttested();
     return {
       user_confirmed_18: confirmed,
-      non_clinical_use: confirmed,
-      media_transfer_consent: confirmed,
-      professional_business_use: false,
+      professional_business_use: confirmed,
       profile_state: state,
       clinical_profile: clinical,
       protected_mode: !!(policy&&policy.protectedMode),
@@ -22528,7 +22186,7 @@
     };
   }
   function aiLocalOnlyReason(ctx) {
-    if (!ctx.user_confirmed_18 || !ctx.non_clinical_use || !ctx.media_transfer_consent) return "Не подтверждены возраст 18+, немедицинское использование и согласие на передачу данных: варианты будут рассчитаны локально без отправки данных в Gemini.";
+    if (!ctx.user_confirmed_18 || !ctx.professional_business_use) return "Не подтверждены возраст 18+ и профессиональная или деловая цель: варианты будут рассчитаны локально без отправки данных в Gemini.";
     if (ctx.clinical_profile) return ctx.protected_reason || "Для выбранного защищённого профиля варианты будут рассчитаны локально без отправки данных в Gemini.";
     return "";
   }
@@ -26314,7 +25972,7 @@
   function updateSkipLinks(){
     var links=d.querySelectorAll('body>.skip-link'),first=links[0],second=links[1],isWorkspace=workspace(),current=route();
     [first,second].forEach(function(link){if(!link)return;if(!link.getAttribute('data-workspace-original-href')){link.setAttribute('data-workspace-original-href',link.getAttribute('href')||'');link.setAttribute('data-workspace-original-text',text(link));}});
-    if(isWorkspace&&current==='profile'){if(first){first.setAttribute('href','#needs_sex');first.textContent='Перейти к основным данным профиля';}if(second){second.setAttribute('href','#needs_calc_btn');second.textContent='Перейти к расчёту потребностей';}}
+    if(isWorkspace&&current==='profile'){if(first){first.setAttribute('href','#needs_person_name');first.textContent='Перейти к данным человека';}if(second){second.setAttribute('href','#needs_calc_btn');second.textContent='Перейти к расчёту потребностей';}}
     else if(isWorkspace&&current==='ration'){if(first){first.setAttribute('href','#globalSearchSection');first.textContent='Перейти к поиску продуктов';}if(second){second.setAttribute('href','#rationSection');second.textContent='Перейти к составу рациона';}}
     else if(isWorkspace){if(first){first.setAttribute('href','#navigationShellContext');first.textContent='Перейти к текущему разделу';}if(second){second.setAttribute('href','#navigationShell');second.textContent='Перейти к навигации';}}
     else{[first,second].forEach(function(link){if(!link)return;link.setAttribute('href',link.getAttribute('data-workspace-original-href')||'#mainContent');link.textContent=link.getAttribute('data-workspace-original-text')||'Перейти к содержимому';});}
