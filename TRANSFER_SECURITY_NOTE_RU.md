# Безопасность переносимой копии

Реальные значения Gemini API credentials намеренно удалены из `api/gemini-secret.php`. Исходный приватный архив пользователя не изменён. Для запуска AI-функций нужно отдельно предоставить серверный секрет или использовать предусмотренную конфигурацию окружения.
